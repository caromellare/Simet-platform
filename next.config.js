/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['static.metricool.com', 'media.licdn.com', 'scontent-lhr8-1.xx.fbcdn.net'],
  },
}

module.exports = nextConfig
